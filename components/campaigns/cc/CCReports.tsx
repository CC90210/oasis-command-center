"use client";

/** CCReports — account-wide analytics. Full build lands in Phase C. */
import { Card } from "@/components/Card";

export function CCReports() {
  return (
    <Card>
      <div className="text-[12px] text-fg-dim">Reports &amp; analytics are being wired up. Per-campaign metrics are already available from the Campaigns tab (click any campaign).</div>
    </Card>
  );
}
