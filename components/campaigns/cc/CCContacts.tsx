"use client";

/** CCContacts — contacts, lists, tags, custom fields, segments, unsubscribes. Full build lands in Phase D. */
import { Card } from "@/components/Card";

export function CCContacts() {
  return (
    <Card>
      <div className="text-[12px] text-fg-dim">Contacts &amp; lists management is being wired up (Phase D). Audience selection for sends already works from the Compose tab.</div>
    </Card>
  );
}
