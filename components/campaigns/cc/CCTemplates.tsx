"use client";

/** CCTemplates — saved reusable email templates gallery + editor. Full build lands in Phase E. */
import { Card } from "@/components/Card";

export function CCTemplates() {
  return (
    <Card>
      <div className="text-[12px] text-fg-dim">The template gallery is being wired up (Phase E). The SunBiz + cold-outreach template libraries are already available in the Compose tab.</div>
    </Card>
  );
}
